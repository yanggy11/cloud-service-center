server:
  port: 1988
spring:
#  profiles: host1
  application:
    name: cloud-service-center
eureka:
  instance:
    hostname: host1
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false